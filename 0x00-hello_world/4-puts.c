#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Textstr [] = "Progrqamming is like building a multilingual puzzle";
	puts(Textstr);
	return (0);
}
