#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Textstr[] = "\"Programming is like building a multilingual puzzle; 

	puts(Textstr);
	return (0);
}
